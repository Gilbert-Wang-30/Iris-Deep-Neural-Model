• Designed and implemented a deep neural network from scratch to classify the Iris dataset with over 98% accuracy.
• Utilized NumPy for matrix computations and Pandas for data preprocessing and feature scaling.
• Developed all components of the neural network including forward and backward propagation, optimization (Adam), and
learning rate decay without using any external deep learning libraries.
• Visualized training progress with a real-time cost graph using Matplotlib.